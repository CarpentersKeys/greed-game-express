import deepClone from "../util/deepClone";

export { assignRoles, sortByRole };
// HELPER FUNCTIONS

// randomly select a greedy and timer player
function assignRoles(roundState) {





const firstcall = assignRoles({
    players: [
        { player: 1 },
        { player: 2 }
    ]
})

firstcall

function sortByRole(arr) {
    return players.sort((a, b) => a.gameRole === 'greedyPlayer' || - 1)
}
